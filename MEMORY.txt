# MEMORY.md — medprep

**THIS FILE OVERRIDES AI MEMORY.**
If your stored memory contradicts this file, your memory is wrong.
If this file contradicts `tree app /F`, the disk is right — update this file.
Never assume a feature is unbuilt. Check first.
Never assume a bug is fixed. Check the files on disk, then the browser.

Repo: https://github.com/Abhidatla203/medprep
Path: C:\Users\abhid\Desktop\medprep
Owner: Abhidatla203

**Last updated:** 25 September 2026
**Project status:** ATTENDANCE REBUILD COMPLETE AND VERIFIED.
Bug queue closed. Test matrix rounds 1–5 passed. Debug route deleted.
Next work is a deliberate choice, not a rescue.

**IGNORE / DO NOT TREAT AS CURRENT TRUTH:**

- `ATTENDANCE_REBUILD_V2.md` — scratch file, gitignored.
- `ATTENDANCE_REBUILD_SPEC.txt` — design intent only. Never saved to disk.
  Where it conflicts with this file or disk, disk + this file win.
- `ATTENDANCE_REBUILD_PROGRESS.md` — if present, a session handoff note only.
  This file is the standing project file.
- Any chat history saying "rings do not match marks", "debug page required",
  "store.ts and generate.ts not inspected", or "compile unconfirmed".
  All of that was true on 24 September and is FALSE as of 25 September.

---

## 1. HOW TO WORK WITH ME

- No coding experience. Explain in plain language.
- Slower pace. Less detail per message. Too much at once is hard to process.
- **FULL REPLACEMENT FILES ONLY.** Never "find line 40 and change it".
  Partial edits have broken this project repeatedly. No exceptions.
- One file at a time. One feature at a time.
- Exact copy-paste PowerShell commands, tailored to the real folder structure.
- Before the code: a short table of what's changing and why.
- Heavy inline comments in the code explaining the REASONING, not the mechanics.
  Those comments are how this project survives context loss.
- If you need an existing file, ASK. Do not guess at export names.
- No back-and-forth clarification loops. Ask at most one question, up front.
- Brainstorming: I give my vision -> you suggest changes -> I assess and reply.
- **Do not rewrite RingGrid or AttendanceRing without a stated defect.**
  The rendering bug was found and fixed. Further rewrites are regression risk.

---

## 2. STACK (verified)

- Next.js 16.3.5 (Turbopack), TypeScript, Tailwind v4
- Storage: localStorage only. No backend.
- Path alias: `"@/*": ["./*"]` (root-relative)
- Dates: store ISO `YYYY-MM-DD`, display dd/mm/yyyy
- University: NTRUHS only for now
- Design principle: **store raw facts, compute views at read time.**
  Nothing derived is ever persisted. That is what makes stale numbers impossible.

**Tailwind v4 hard rule:** never write `bg-[--color-safe]`. That generates **nothing**.
Use real theme utilities (`bg-safe`) or explicit inline colours. This caused Present
buttons to go blank (white text on white).

---

## 3. ATTENDANCE MODULE — BUILD STATUS (all verified in browser)

| #   | File                                            | State                                                                       |
| --- | ----------------------------------------------- | --------------------------------------------------------------------------- |
| 1   | `app/attendance/types.ts`                       | DONE. `SafetyBand` union unchanged: safe / warning / danger / critical      |
| 2   | `app/attendance/calculate.ts`                   | DONE. Section 6 `computeYear()` three-pass rewrite. `targetUnreachable` gated on remaining runway |
| 3   | `app/attendance/store.ts`                       | DONE. Cleared of suspicion. Backup/restore lives here, preserves entry ids  |
| 4   | `app/attendance/generate.ts`                    | DONE. Cleared of suspicion. Was starved by the one-day term, not broken     |
| 5   | `app/globals.css`                               | DONE. Tiles, modal, ring tokens                                             |
| 6   | `app/attendance/components/WeekStrip.tsx`       | DONE. Pinned compact card + long-press overlay, tile times on the rail      |
| 7   | `app/attendance/components/MarkList.tsx`        | DONE. Colour-coded P/A/C                                                    |
| 8   | `app/attendance/components/RingGrid.tsx`        | DONE. Delegates drawing to AttendanceRing. Five display standings           |
| 9   | `app/attendance/components/AttendanceRing.tsx`  | DONE. Presentational only — no store, no calculator, no Tailwind            |
| 10  | `app/attendance/setup/TermPanel.tsx`            | DONE. The only writer of `settings.term` in the app                         |
| 11  | `app/attendance/page.tsx`                       | DONE. WeekStrip + MarkList + RingGrid, hydration gate, page-scoped `runMigrations()` |

`app/attendance/debug/page.tsx` — **DELETED** after the matrix passed. It was a
temporary 7-panel diagnostic. Recreate only against a new, named defect.

**TypeScript:** `npx tsc --noEmit` clean. ESLint / React Compiler clean.
Error-count history (historical only): 45 -> 5 -> 42 -> 21 -> 18 -> 0.

---

## 4. THE FIVE RULES OF THE REBUILD

Every one of these exists because the v3 code got it wrong.

1. **EXACT RATIO FOR EVERY DECISION.**
   v3 rounded to one decimal, then compared. 74.96% became 75.0, scored "safe",
   and reported zero classes needed — to a student below the line.
   `ratio` is the unrounded fraction and is the only thing compared **in calculate.ts**.
   `percentDisplay` exists solely to be printed.
   **Ring SVG must use `attended / conducted`, never `ratio` or `percentDisplay`.**

2. **NO POOLED NUMBERS.**
   No overall percentage. No subject percentage. No year percentage.
   A pooled 78% can read green while Pathology practical sits at 68% and the
   student is debarred. `OverallResult`, `SubjectResult.percent` and
   `YearResult.percent` are DELETED. Do not reintroduce them.

3. **TWO THRESHOLDS, RESOLVED PER SUBJECT PER TYPE.**
   Defaults: theory 75%, practical 80%, clinical 80%.
   Overridable per subject per type, in SETTINGS ONLY, never in setup.
   Store is SPARSE — a missing key means "use the default", not zero.

4. **PURE CORE.**
   `calculate.ts` sections 1–6 are pure functions. No storage, no clock.
   Sections 7–8 are the thin storage-backed layer the UI calls.

5. **ONE PASS.**
   Sessions bucketed by `subjectId::category` in a single traversal.
   Both `calculate.ts` and `generate.ts` memoise on `DataVersion`.
   `setMark()` bumps DataVersion — verified.

---

## 5. THE MARKING MODEL (universal, not configurable)

```
present        -> attended += weight,  conducted += weight
absent         -> attended += 0,       conducted += weight
not-conducted  -> nothing. 0/0. Leaves both sides. UI label: "Cancelled"
unmarked       -> currently excluded from the denominator (counted only for "N unmarked")
```

Stored value stays `'not-conducted'`. The UI says "Cancelled". Renaming the
stored value would orphan every existing mark for zero benefit.

**Unmarked policy is a product decision, not an accident.** Past unmarked classes
currently do **not** count as conducted. So Present + Present + Unmarked + Unmarked
= 2/2 = 100%. If the UI implies otherwise, that is a bug. Decide and document
before changing the unmarked branch.

---

## 6. WHAT v4 CHANGED FROM v3

| Change                                         | Why                                                           |
| ---------------------------------------------- | ------------------------------------------------------------- |
| `ExtraClass.countsTowardDenominator` per entry | Was one global setting that swept through regular classes too |
| `settings.term.targetPercent` DELETED          | One number cannot express 75% theory / 80% practical          |
| `settings.extraClassPolicy` DELETED            | Now per-entry                                                 |
| New `ThresholdStore`                           | Sparse, per year/subject/category                             |
| `DataVersion` counter                          | Memo key for both caches                                      |
| TRAP 8 exclusion lock REMOVED                  | Exclusion deletes nothing and reverses instantly              |
| Blockouts outrank stale `absent` marks         | A class that didn't happen cannot be an absence               |
| Regular sessions filtered by `workingDays`     | A 6-day college was generating Sunday classes                 |
| Backup preserves entry ids                     | Old importer minted fresh ids, orphaning every mark           |

**The structural safety mechanism:** extra sessions HAVE
`countsTowardDenominator`. Regular and posting sessions leave it `undefined`.
Recalculating extras is therefore _incapable_ of reaching a scheduled class —
enforced by the type system, not by carefulness.

---

## 7. CURRENT PHASE — STABLE. CHOOSE THE NEXT FEATURE DELIBERATELY.

The requirement that was failing is now true in the browser:

> Marks made in the UI drive generated sessions, calculated percentages,
> week tiles, and rings.

Nothing is blocking. Before starting anything new, read section 11 (traps) and
section 8 (naming collisions) — they are still live constraints.

### MEDIUM — carried forward, not urgent

- `runMigrations()` is called in `app/attendance/page.tsx` before `setMounted(true)`,
  with a StrictMode ref guard. Still **page-scoped**. First visit to `/settings` or
  `/attendance/setup` can read pre-migration data. Proper home: root layout client
  effect. Do it with the Turbopack font issue in `layout.tsx`.
- `useAttendance.ts` — not audited. May reference deleted v3 API.
- `app/settings/attendance/page.tsx` — may still read `targetPercent`.
- Thresholds belong in SETTINGS, not setup. Settings UI may still lack that.

### LOW

- `extraPolicy` fallback in `calculate.ts` may be dead. Delete after more real use.
- `datetime.ts` comment referencing `DateField` — cosmetic.
- `subjectShort()` codes are 4–6 chars; may truncate in week cells.
- Two component folders, two store patterns, two attendance settings screens.

---

## 8. TWO NAMING COLLISIONS — READ BEFORE WRITING UI

**1. Safety band names differ between code and CSS.**

```
types.ts SafetyBand : safe | warning | danger | critical
CSS tokens          : safe | watch   | risk   | critical
```

Never write `` `chip-${band}` `` — it produces `chip-warning`, which does not
exist, and the chip renders unstyled. Map it explicitly, in one place.

**A third vocabulary now exists: DISPLAY STANDINGS in RingGrid.** See section 10.
Three vocabularies, one mapping site each. Do not add a fourth.

**2. `--color-practical` is the SAME GREEN as `--color-safe` (#0f9b6c).**

Fatal on the week strip, where green means PRESENT — a practical tile would
read as "attended" purely by being a practical.

**Rule: the week strip uses STATUS colour for the fill only.**
Class type is a stripe + letter (T / P / C), not the fill hue.

Category hues (hard-coded on purpose, because CSS classes failed silently):

```
theory    arc #7c3aed
practical arc #ea580c
clinical  arc #0891b2
```

**TRACKS ARE NO LONGER TINTED.** All ring tracks are neutral grey #E8E8ED
(empty track #F0F0F4). This was the ring bug. A tinted track drew a complete
pale-violet circle at 0/0 that was indistinguishable from a full violet arc at
phone size. COLOUR = ATTENDED. GREY = MISSED. Do not reintroduce tinted tracks.

---

## 9. DESIGN SYSTEM — `app/globals.css`

"Ink & Aurora". Warm paper (#fbfaf7), hairlines over shadows, tabular numbers
everywhere. No automatic dark mode — the `prefers-color-scheme` block was
removed deliberately.

**Tokens:** `--color-canvas`, `--color-surface`, `--color-surface-sunk`,
`--color-surface-raised`, `--color-ink`, `--color-ink-soft`, `--color-ink-muted`,
`--color-ink-faint`, `--color-line`, `--color-line-strong`, `--color-brand`,
`--color-brand-hover`, `--color-brand-soft`, `--color-brand-line`,
`--color-brand-ink`, `--color-safe/-soft/-line`, `--color-watch/-soft/-line`,
`--color-risk/-soft/-line`, `--color-critical/-soft/-line`, `--color-theory`,
`--color-practical`, `--color-clinical`, `--color-ring-track`, `--color-ring-empty`,
`--radius-card/-field/-sheet/-modal/-pill`

**Classes:** `.card`, `.card-interactive`, `.display`, `.eyebrow`, `.tnum`,
`.btn` + `-primary/-ghost/-quiet/-danger`, `.field`, `.field-select`, `.seg`,
`.seg-on`, `.chip` + `-brand/-safe/-watch/-risk/-critical/-neutral`, `.sheet`
`-head/-body/-foot/-grip/-scrim`, `.modal-layer`, `.modal-scrim`, `.modal-card`,
`.modal-head`, `.modal-body`, `.modal-close`, `.tile` + `-present/-absent/-unmarked/`
`-future/-cancelled/-offgrid`, `.day-today`, `.break-row`, `.ring-track`,
`.ring-empty`, `.ring-arc`, `.ring-theory/-practical/-clinical`, `.ring-tick`,
`.ring-label-*`, `.aurora`, `.animate-rise`

**Modal:** centred, blurred scrim, spring scale, Escape, scrim tap, body
`data-modal-open="true"` locks scroll.

**The sheet layout that keeps Save buttons reachable:** flex column, three
children — shrink-0 head, `flex-1 min-h-0 overflow-y-auto` body, shrink-0 foot.
The footer is a SIBLING of the scroll area, never a sticky child of it.
`min-height: 0` on the body is the part that gets forgotten.

---

## 10. UI RULES BY COMPONENT

### Week strip

- READ-ONLY. Renders sessions, computes nothing. Marking happens below.
- NO PERCENTAGES. Ever. Its job is "have I logged everything?", not "am I safe?".
- **ROWS = days. COLUMNS = time.** Matches printed college timetables. Overflow
  goes sideways, not taller.
- One row per WORKING day. A non-working day is ABSENT from the grid.
- Width proportional to duration (segment model: every start/end boundary).
- Breaks INFERRED. Uncovered span >= 20 min. Consecutive breaks merge. Shorter
  gaps are changeover. Break DURATION LABELS were removed — the dashed divider
  plus the two adjacent timestamps already state it twice.
- Time labels ALWAYS include am/pm: `9am`, `10:30am`, `2pm`. Never bare `2`.
- Times live ON THE RAIL, not on every tile: start at the left edge of a class
  column, end only after a gap or on the last column.
- Fill = status. Stripe + T/P/C = class type.
- The three greys, ordered by how much each wants a tap:
  `unmarked` light+solid -> `future` palest+dashed -> `cancelled` dark+struck.
- Tile segment count equals weight. A posting counted as 3 draws 3 segments.

**Compact card vs overlay — two anchors, never shared.**
The compact strip is PINNED to this week and has no arrows. A long press
(450ms, 10px tolerance) expands a blurred overlay which owns `browseAnchor`
and discards it on close.

**Swipe lives only in the overlay.** Rationale: the strip is already a
horizontal scroller, and a swipe handler layered over a scroller fights it and
the user loses. In the overlay the swipe follows the finger, glides past 56px,
and the incoming week enters from the opposite side. Filters retained: drag must
be mostly horizontal (dx > dy) so vertical page scrolling never changes week,
and must clear 56px so a wobbly tap does nothing. `touchAction: pan-y`.

**Date jump** is a transparent `input type=date` over the range label, so a
phone opens the OS wheel instead of a hand-built HTML calendar.

**`localAnchor` is a FALLBACK, not a second owner.** `page.tsx` owns
`selectedDate`; navigating always calls `onSelectDay`. `localAnchor` only covers
a parent that does not feed the date back through `anchorDate` — without it the
strip navigates then snaps back, which reads as a broken button. Cleared by an
effect on `parentAnchor`, so a controlled parent always wins.

**Outside-term warning.** Navigating past the term edges shows an amber note:
marks made out there are saved but never reach the rings. Silently rendering a
normal grid is exactly the deception that cost this project weeks.

### Mark list

- ONE ROW PER SESSION whatever the weight. One tap. "counts as 3" chip shown
  only when weight > 1.
- Cancelled button is SMALLER than Present/Absent.
- NO DEFAULT SELECTION.
- Tapping the same button again unmarks.
- Colour-coded at rest and selected (green / red / grey). Must stay visible.
- "Mark all cancelled" — only for days that have already happened.
- Quick add writes a one-off `ExtraClass` with `countsTowardDenominator: true`.
  No Cancelled option on quick add.

### Ring grid

- One ring per REAL category the subject has. Theory outermost.
  Do not assume a theory+practical pair. That dropped real data.
- **Ring colour = IDENTITY, always.** Safety lives on the LABEL + threshold notch.
- Geometry: `fraction = conducted > 0 ? attended / conducted : 0`.
- Empty (`conducted === 0`) draws NO ARC AT ALL — a faint dotted track and
  nothing more. 0/0 can never again read as 100%.
- NEVER shrink rings to fit more subjects. Min diameter ~96px.
- Numbers appear in a **modal**, not a card shoved under the grid.
  The same ring is shown large at the top of that modal.
- No helper paragraph under "Where you stand". Category dots are enough.
- Two columns on mobile. No tile borders — depth from surface + one soft shadow.

**FIVE DISPLAY STANDINGS. `targetUnreachable` is checked FIRST.**

```
targetUnreachable -> "Critical"     (any band)
critical          -> "Well behind"  (downgraded)
danger            -> "Behind"
warning           -> "Watch"
safe              -> "On track"
```

Grid sort order, modal chip and per-category percentage colour all use this
same mapping. **DISPLAY ONLY** — no band, ratio, threshold or count is
recalculated in RingGrid. `calculate.ts` is the sole source of arithmetic.

Reasoning: **the distinction is REACHABILITY, NOT DISTANCE.** `bandFor()`
returns `critical` for anything more than `dangerMargin` below threshold, so a
subject at 50% in week four of a six-month term qualified — despite being
comfortably recoverable. Three of four subjects reading "Critical" at once is a
warning that describes nothing.

### AttendanceRing.tsx

Presentational only. No store, no calculator, no Tailwind. Gradient arcs, round
caps, `useId` for unique gradient ids — duplicate ids make every ring inherit
the first one's colours, a classic SVG trap.

Silhouette constants, each earned:

- `STROKE` 13, `GAP` 5, `R_OUTER` 51. Thin hoops with a large hole read as
  wireframe; Activity rings are ~22% of radius and that weight is most of why
  they look solid.
- Threshold notch inset to `STROKE/2 - 2.5` with **butt caps**. Round caps add
  ~half the line width beyond each endpoint, which produced visible spurs at
  9 o'clock. A mark ON the ring, never a spur off it.
- Empty track is SOLID #F0F0F4. `strokeDasharray '1 7'` + round caps produced a
  scalloped, beaded rim. Lightness says "empty"; texture costs the outline.
- Arc dash trimmed by `STROKE/2` so the round start cap completes inside the band
  instead of mounting the track edge at 12 o'clock.
- `shapeRendering: 'geometricPrecision'` — stops curve points snapping to the
  pixel grid, which makes small circles look polygonal.
- `flexShrink: 0` — stops a narrow grid column squashing the SVG into an oval.

### Setup — TermPanel

Sits under `HoursCard`. Warns on: one-day term, span under 14 days, end before
start, today before start, today after end. Warnings name the CONSEQUENCE
("anything you mark outside this range will not reach your rings"), not the rule.

**Uses LOCAL DRAFT STATE + commit on blur**, unlike the rest of setup which
writes immediately. A date input fires `onChange` per keystroke; typing a year
would write 0002 / 0020 / 0202 / 2027, bumping `DataVersion` and regenerating
the term four times. The exception is deliberate.

### Page

- Owns `selectedDate`. Strip and list stay in sync.
- Mount gate: no store reads until after first client pass + migrations.
- This year's exam subjects in the main grid. **No other-subjects dropdown.**
- Non-exam subjects stay tracked in data, not on this screen.

---

## 11. THE TEN TRAPS (+ session traps)

1. **Category from the slot, never from history.** A posting replaced by a
   theory class produces a THEORY session.
2. **An excluded subject's opening balance is excluded too.**
3. **`countsToward: 'both'` = ONE class satisfying TWO requirements.**
4. **Monday = 0, NOT Sunday.** `(jsDay + 6) % 7`.
5. **Never use `toISOString().slice(0,10)`.** Hand-roll local date.
6. **College hours enforced in the DATA layer**, not just hidden in the UI.
7. **Overlapping blockouts mark a date ONCE.**
8. ~~Exam subjects cannot be excluded~~ — **REMOVED in v4.**
9. **Curriculum says POSSIBLE; timetable says REAL.** Never show "ENT Practical — 0%"
   for a practical that was never on the timetable.
   **NARROWED, not deleted:** non-exam subjects still show only categories with
   real data. EXAM subjects are exempt — they own a fixed outer/inner ring slot,
   and a missing inner ring is worse than an empty one.
10. **Opening balance REPLACES, never adds.**

**Plus:**

- Session ids must be DETERMINISTIC — `s_reg_{entryId}_{date}` (and the extra
  equivalent). No `Date.now()`, no `Math.random()`. Marks are keyed by session id.
- **Tailwind v4 `bg-[--color-*]` is dead.** Use `bg-safe` or inline styles.
- **`setMark` id must equal generated session id.**
- **Caches must die when marks change.**
- **Do not trust screenshots alone.** Compare store, generated sessions, calculator, UI.
- **A tinted track is a lie at small sizes.** See section 8.

---

## 12. ENVIRONMENT GOTCHAS

**Turbopack + Google Fonts intermittent build failure.**
`Module not found: Can't resolve '@vercel/turbopack-next/internal/font/google/font'`
Open upstream bug (vercel/next.js#99114). Not your code.
Fix order: clear `.next` -> drop `--turbopack` -> self-host Inter via
`next/font/local` (the only permanent fix).

**Zombie dev server.** Deleting `.next` while a server runs leaves the port
held. Stop PID, or:

```powershell
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
```

**PowerShell `**` is NOT recursive.** Use `Get-ChildItem -Recurse`.

---

## 13. PROJECT RULES

- Two curricula, intentionally separate:
  - **University** (`app/data/universities/ntruhs/curriculum.ts`) -> question
    banks, MCQs, analysis, planner.
  - **Attendance** (`lib/attendance/curriculum.ts`) -> classes and timetable.
- Class types: theory, practical, clinical.
- Daily marking: Present, Absent, Cancelled.
- Clinical postings UI must explain when the student's year has none.
- Backups: GitHub **and** offline.

---

## 14. KNOWN ISSUES (not blocking)

1. **Two component folders** — `app/components/` and root `components/`.
   Root now holds only `attendance/DateField.tsx` (used by
   `app/settings/academics/page.tsx`). New attendance UI goes in
   `app/attendance/components/`.
2. **Two store patterns** — `app/store/settings.tsx` (Context) vs
   `app/attendance/store.ts` (plain module).
3. **Two attendance settings screens** — `app/settings/attendance/page.tsx` and
   `app/attendance/setup/page.tsx`.
4. **Thresholds belong in SETTINGS, not setup.**

---

## 15. TESTING MATRIX — rounds 1–5 PASSED 25 Sep 2026

Re-run this whole matrix before shipping, and after any change to
store.ts, generate.ts, calculate.ts or AttendanceRing.tsx.

| Test                 | Expected                                        | Status |
| -------------------- | ----------------------------------------------- | ------ |
| Present click        | Row, week tile, ring update                     | PASS   |
| Absent click         | Row, week tile, ring update                     | PASS   |
| Cancelled click      | Row, week tile, ring update                     | PASS   |
| Repeat active mark   | Returns to unmarked                             | PASS   |
| Refresh              | Marks persist                                   | PASS   |
| DataVersion          | Changes after mark                              | PASS   |
| Subscriber           | Fires after mark                                | PASS   |
| Session id           | Same in setMark and generate                    | PASS   |
| Theory %             | Independent, matches present/(present+absent)   | PASS   |
| Practical/clinical % | Independent                                     | PASS   |
| Ring arc             | Proportional to attended/conducted              | PASS   |
| Ring track           | Neutral grey remainder, visible                 | PASS   |
| Empty category       | No arc drawn; not labelled Critical             | PASS   |
| Week type coding     | Stripe + T/P/C visible                          | PASS   |
| Time labels          | am/pm                                           | PASS   |
| Duration / breaks    | Long classes wider; breaks visible              | PASS   |
| Hydration            | No mismatch                                     | PASS   |
| TypeScript           | Zero errors on a fresh `tsc`                    | PASS   |
| ESLint / React Compiler | Clean; hooks ordered                         | PASS   |

Reference counts from the verified dataset (useful as a regression fixture):
medicine/theory 2/4, surgery/theory 2/3, surgery/clinical 3/5,
obg/theory 2/2, paediatrics/theory 2/2.

---

## 16. SESSION START PROMPT

```
I'm working on the attendance module in my Next.js app at
C:\Users\abhid\Desktop\medprep. MEMORY.md is attached — read it fully before
writing anything. It's the single source of truth.

STATUS: the attendance rebuild is COMPLETE and VERIFIED. The bug queue is
closed and the section 15 test matrix passed. Do NOT re-investigate the
rings/marks mismatch, the one-day term, or the missing categories — all three
are fixed and documented in section 18.

How I want you to work:
- FULL REPLACEMENT FILES ONLY. Never append a block or change specific lines.
- One file at a time. I'll run npx tsc --noEmit and paste the output.
- Before the code, a short table of what's changing and why.
- Heavy inline comments explaining the reasoning.
- If you need an existing file, ASK. Do not guess exports.

Before proposing anything, read section 11 (the traps) and section 8 (the
naming collisions). They are still live constraints.
```

---

## 17. SESSION END — 60 seconds, non-negotiable

1. Append a CHANGELOG entry below.
2. Run:

```powershell
cd C:\Users\abhid\Desktop\medprep; npx tsc --noEmit; git add -A; git commit -m "session: <what changed>"; git push
```

If the working tree is clean, still `git push`.

Stop the dev server first if needed:

```powershell
Get-NetTCPConnection -LocalPort 3000 -ErrorAction SilentlyContinue |
  ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }
```

Or Ctrl+C in the window running `npm run dev`.

---

## 18. CHANGELOG — append only, newest at bottom

[2026-09-20] Settings overlay work begun.
[2026-09-22] Settings overlay committed: app/settings/, 9 files, 1218 insertions (824da48).
[2026-09-22] Disk audit via tree /F. All three handover docs were each partially
correct; no code was ever lost.
[2026-09-22] Deleted dead file app/attendance/page.old.tsx.bak.
[2026-09-22] MEMORY.md created as single source of truth, overriding AI memory.
[2026-09-22] AGENTS.md and CLAUDE.md already point to MEMORY.md.
[2026-09-22] F5 persistence test: /attendance redirects to /attendance/setup.
Marking page missing — earlier agent built only setup + redirect.
[2026-09-22] DECISION: rebuild /attendance from scratch.

[2026-09-24 REBUILD SESSION] Full v4 rebuild of the attendance logic layer.
Removed 1,618 lines of orphaned pre-rewrite code (56fcc3c):
components/attendance/{ClassEditorSheet,PostingScheduler,TimeField,WeekGrid}.tsx
and lib/attendance/backup.ts. DateField.tsx restored (3a9ab7c) — still used by
app/settings/academics. Backup/restore moved into store.ts, preserving entry ids.
Rewritten to v4: types.ts, store.ts, calculate.ts, generate.ts.
Added: globals.css tile + ring tokens, WeekStrip.tsx, MarkList.tsx.
page.tsx replaced with a temporary stopgap so the dev server compiles.
Key fixes: exact-ratio comparisons (the 74.96% bug), all pooled numbers deleted,
per-subject-per-type thresholds, per-entry extra-class denominator policy,
workingDays filtering, DataVersion-keyed caches, blockouts outranking stale
absence marks, deterministic ids verified throughout. tsc clean.

[2026-09-24 UI ASSEMBLY + BUG CHASE] RingGrid.tsx and the real page.tsx written
and wired. runMigrations() called on the attendance page before first store read.
Hydration gate added. WeekStrip rotated to days-as-rows. MarkList Tailwind v4
blank-button bug fixed. ENDED BLOCKED: marked attendance did not match rings.

[2026-09-25 DIAGNOSTIC] Branch main, commit e9ff51f. tsc clean.
Temporary app/attendance/debug/page.tsx built as a controlled 7-panel diagnostic.

[2026-09-25 BUG 1 DIAGNOSED — ONE-DAY TERM] 20 of 24 week sessions missing from
the year dataset; OBG absent from "Where you stand". ROOT CAUSE: no screen in
the entire app ever wrote settings.term. Grep of app/ + lib/ proved every
reference was a read, a default, or a legacy migration — store.ts:278 and
307-308 both defaulted to todayISO(); generate.ts:686, 699, 727-728 read the
term to build the year window; PostingsPanel dates are unrelated posting dates.
The only term that had ever existed on a real device was today -> today.
A MISSING FEATURE, not a user error. Unblocked temporarily via browser console
on key medprep.attendance.settings.v4.

[2026-09-25 BUG 2 FIXED — calculate.ts SECTION 6] computeYear() rewritten to
three explicit passes: PASS 1 real session buckets (curriculum-illegal
categories dropped), PASS 2 every EXAM subject completed to its full legal
category pair with 0/0, PASS 3 opening-balance-only categories on NON-EXAM
subjects. bySubject is now Map<SubjectId, Map<ClassCategory, CategoryResult>>;
the local put() helper no-ops on an existing key, so a populated category can
never be overwritten by a later pass. TRAP 9 narrowed, not deleted.
VERIFIED in browser: no CATEGORY MISSING, no SUBJECT MISSING in diagnostic
section 5. medicine and paediatrics return theory + clinical. All previously
passing counts unchanged. tsc clean. Committed.

[2026-09-25 RING RENDERING FIXED] Symptom: every ring looked full regardless of
attendance. ROOT CAUSE was not the arc maths, which was correct all along — the
TRACK was tinted per category (theory track #cfc7dd against arc #7c3aed), so a
0/0 ring drew a complete pale-violet circle indistinguishable from a full arc at
phone size. FIX: all tracks neutral grey #E8E8ED. Drawing delegated to the new
app/attendance/components/AttendanceRing.tsx, which draws NO arc when
conducted === 0. RingGrid rewritten: outer = theory, inner = practical/clinical;
geometry from attended/conducted only; tile borders dropped for soft elevation;
two columns on mobile; hairline threshold notch; same ring shown large in the
detail modal. tone="category" retained.

[2026-09-25 RING SILHOUETTE FIXED] "Circle doesn't look too circly." Geometry
was right; the OUTLINE was broken by three details — notch overhanging the rim
via round caps, a beaded '1 7' dash on the empty track, and the arc's round
start cap mounting the track edge at 12 o'clock. All three corrected. STROKE
11->13, GAP 7->5, R_OUTER 50->51. Added shapeRendering geometricPrecision and
flexShrink 0.

[2026-09-25 BAND VOCABULARY FIXED — RingGrid.tsx] Three of four subjects read
"Critical" at once. Cause semantic, not arithmetic: bandFor() returns critical
for anything more than dangerMargin below threshold. THE DISTINCTION IS
REACHABILITY, NOT DISTANCE — calculate.ts already computed targetUnreachable and
it was being ignored. Five display standings introduced, targetUnreachable
checked first. Display only.

[2026-09-25 targetUnreachable NARROWED — calculate.ts] Surgery clinical is a
posting block (21-25 Sep); past its end date remainingWeight === 0, so
isUnreachable(3, 5, 0, 80) returned true. Mathematically correct, semantically
wrong — a finished posting block is not a finished term. targetUnreachable now
requires remainingWeight > 0. ACCEPTED SIDE EFFECT: at genuine term end nothing
will ever say Critical. If an end-of-term "finalised / below requirement" state
is wanted, add an explicit isFinalised flag driven by settings.term.endDate —
do NOT revert this.

[2026-09-25 "CRITICAL" RESOLVED — NOT A BUG] Only General Surgery still reads
Critical and it earns it: 3/5 at an 80% threshold needs x >= 5 consecutive
classes for (3+x)/(5+x) >= 0.8, and fewer than 5 remain scheduled. ROOT CAUSE IS
DATA: only the 21-25 Sep posting block is entered. Adding the next block restores
runway and the label self-corrects to "Well behind". DO NOT soften
targetUnreachable further to make this label go away.

[2026-09-25 BUG 1 PERMANENTLY FIXED — new file setup/TermPanel.tsx] The term is
now settable in the UI. Local draft state + commit on blur, deliberately unlike
the rest of setup, because a date input fires onChange per keystroke and typing
a year would write 0002/0020/0202/2027 and regenerate the term four times.
LESSON RECORDED: a required setting with a plausible default and no UI is worse
than one with no default. A missing value announces itself; a default of `today`
poisons every downstream calculation while looking reasonable in the debugger.

[2026-09-25 WEEKSTRIP — TILE TIMES + NAVIGATION] Grid floor raised 2.75rem ->
4.5rem. Break duration labels removed from the time header as redundant. Prev/
next week, a "Today" control rendered only when off the current week, and a date
jump implemented as a transparent input type=date over the range label so phones
open the OS wheel. Outside-term amber warning added.

[2026-09-25 WEEKSTRIP — LONG PRESS + PINNED COMPACT CARD] Compact strip pinned
to this week with no arrows; long press (450ms, 10px) expands a blurred overlay
that owns browseAnchor and discards it on close. Two anchors, never shared.
Swipe lives only in the overlay, resolving the gesture conflict with the
horizontal scroller. Times moved to the rail. tsc clean, ESLint/React Compiler
clean (hooks ordered, hint via useSyncExternalStore).

[2026-09-25 MATRIX PASSED — QUEUE CLOSED] Section 15 rounds 1-5 passed.
app/attendance/debug/page.tsx DELETED. Attendance rebuild complete.

[2026-09-25 DOC RECONCILIATION] MEMORY.md reconciled against disk. Removed the
stale PAUSED/BUG-FIXING-ONLY header, the "debug page NOT CREATED" line, the
"compile unconfirmed" warnings and the 18 NOT VERIFIED matrix rows — all
contradicted by this session's verified work. De-duplicated the Bug 2 changelog
entry (present twice) and the truncated first copy of the WeekStrip tile-times
entry. Added AttendanceRing.tsx to the file table, the five display standings to
section 10, and the neutral-track rule to section 8.
